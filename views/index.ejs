<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Notes</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 ">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-700 w-full rounded-md" type="text" placeholder="title goes here" name="title">
                <textarea class="block px-5 py-2 bg-zinc-700 mt-2 resize-none w-full rounded-md" type ="text" placeholder="Write your task details here" name="details"></textarea>
                <input class="mt-2 bg-blue-700 px-5 py-2 rounded-md" type="submit" value="Create task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 wrap ">
           <% if (files && files.length > 0) { %>
                <% files.forEach((file) => { %>
                    <div class="w-72 px-5 py-4 bg-zinc-700 rounded-md">
                        <h2 class="text-2xl text-white"><%= file %></h2>
                        <a class="inline-block mt-2 text-blue-500" href="/file/<%= file %>">Read more</a>
                        <a class="inline-block mt-2 text-blue-500" href="/edit/<%= file %>">Edit name</a>
                    </div>
                <% }) %>
            <% } else { %>
                <p class="px-5 py-4 bg-zinc-700 rounded-md">No tasks found</p>
            <% } %>
            </div>
            
                        
        </div>
    </div>

</body>
</html>